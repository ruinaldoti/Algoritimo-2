export class Node{
    constructor(key){
        this.key = key; // {1 valor do nó}
        this.left = null; // referencia ao nó que é o filho à esquerda
        this.right = null; // referencia ao no que é filho à direita
    }
}